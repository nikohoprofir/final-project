fn main() {
    // Example Rust code
    let mut array = Vec::new();
    array.push(1);
    array.push(2);
    array.push(3);
    
    for i in 0..array.len() {
        println!("{}", array[i]);
    }
}
